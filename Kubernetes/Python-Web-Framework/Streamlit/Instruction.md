## The Streamlit App (No HTML Way)
1. Create a file named `app_streamlit.py.`
2. Install streamlit and run it via its own CLI wrapper
   ```bash
    pip install streamlit
    streamlit run app_streamlit.py --server.port 3005
  ```
Note : Streamlit will automatically open a browser window for you on port 3005.

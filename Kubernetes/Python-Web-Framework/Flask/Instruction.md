## How it works
1. Python code : `app_flask.py`
2. install Flask, and run it:
   ```bash
    pip install flask
    python app_flask.py
   ```

3. Go to `http://localhost:3005` in your browser to see it run.

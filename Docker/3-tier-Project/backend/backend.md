## Frontend
Source Code at below git repo
https://github.com/docker-hy/material-applications/tree/main/example-backend

Use docker file to build annd run
```bash
docker build -t backend .
docker run -d -p 8080:8080 backend
```
